# 互動文字雲

大家一起輸入詞彙，畫面上即時長出一朵文字雲。同一個詞被越多人送出，字就越大。

檔案只有一個：`index.html`。整站沒有建置步驟，複製到任何靜態空間就能跑。

---

## 兩種畫面

同一個網址，帶不帶 `?admin=1` 會看到完全不同的東西。

### 學生端（掃 QR 進來的畫面）

只有一件事可以做：**打字送出**。沒有工具列、沒有任何開關。

- 輸入框比管理端更大，畫面上沒有多餘的說明文字
- 統計只顯示「全班已經送出 N 個想法 · 你送出了 K 個」，**看不到有幾個不同的詞** —— 揭曉前不劇透
- 還沒揭曉時，上方顯示等待畫面（大家已經送出 N），**文字雲連 DOM 都不會生成**，學生翻開發者工具也看不到內容
- 老師按下揭曉的瞬間，畫面會自己亮起來並跳出「文字雲揭曉了！」，不用重新整理
- 保留「撤回我上一個詞」，打錯字時可以救回來

### 管理端（`?admin=1`）

| 元件 | 說明 |
|---|---|
| **一鍵揭曉 / 收回文字雲** | **全班同步**的開關。按下去，每一支手機的畫面同時出現文字雲；再按一次收回。已揭曉時按鈕會轉成墨黑色，狀態一眼可辨。 |
| **預覽** | 只影響**你自己這台**。揭曉前先看一眼文字雲（例如先確認有沒有不適當的字眼），學生端完全不受影響。揭曉之後這個按鈕會自動收起來。 |
| **QR Code** | 顯示這一頁的網址與 QR。學生掃了就直接進到同一個主題。 |
| **投影模式** | 收掉輸入框、進入全螢幕，只留文字雲。滑鼠移到上方仍可叫回工具列。 |
| **主題名稱** | 輸入框上方那一列。改名字後按「切換主題」就會跳到另一個主題；旁邊是「清空這個主題」。 |

## 輸入：以「詞」為主

兩端的輸入框行為相同。**正常用法就是打詞，用逗號（`,` `，` `、`）分隔可以一次送好幾個**，按 Enter 直接進文字雲，中間不多問。

```
合作，溝通，耐心，尊重     → 四個詞直接送出
```

**只有當某一段超過 8 個字時**，系統才判斷「這比較像一句話而不是一個詞」，先拆成關鍵字用紅色標籤列出來、順手濾掉贅詞，**點一下可以取消不要的**，確認後再按「加入 N 個詞」才真的送出。按「取消」的話原本輸入的內容會還回輸入框，不會白打。

```
輸入：我覺得讀完這篇文章最重要的是要能推論作者意圖
抓到：這篇文章 ／ 重要 ／ 推論 ／ 作者 ／ 意圖
```

門檻是**逐段**檢查而不是看整串長度 —— 「合作，溝通，耐心，尊重，負責」有 14 個字，但每一段都是乾淨的詞，不會被攔下來多按一次。單一英文字則放寬到 30 字（`comprehension` 不該被拆）。

斷詞用瀏覽器內建的 `Intl.Segmenter`，沒有外掛任何函式庫、也不會把學生的文字送到任何第三方服務。中英文都可以。

它不是完美的 —— 偶爾會把動詞跟名詞黏在一起（「整理主旨」），或漏掉某個複合詞。**確認那一步就是為了這個存在的**，不合用的點掉就好。

一次最多送出 20 個詞，每個詞最長 30 字。

### 一堂課的流程

1. 老師開 `?admin=1`，按 **QR Code** 投影出來
2. 學生掃碼，開始打想法 —— 他們只看到等待畫面
3. 老師按 **預覽** 確認內容沒問題（學生端還是等待畫面）
4. 老師按 **一鍵揭曉** —— 全班的手機同時亮起來
5. 老師切 **投影模式** 全螢幕討論

## 主題（同時開好幾場）

網址加上 `?room=代號` 就是一個獨立的文字雲，彼此不互相干擾：

```
https://你的網址/?room=六年一班
https://你的網址/?room=期末回饋
```

沒帶參數時用的是 `default` 這個主題。QR Code 會自動帶上目前的主題代號。

## 管理模式怎麼進去

網址加上 `?admin=1`：

```
https://你的網址/?admin=1
https://你的網址/?room=六年一班&admin=1
```

學生掃 QR 拿到的網址**不會**帶 `admin=1` —— QR 產生時會自動拿掉，所以他們拿到的一定是學生端畫面。

> 這只是「不讓人一眼看到」，不是真的權限保護。資料庫對匿名使用者是開放讀寫的，懂技術的學生還是能自己在網址後面加上 `?admin=1` 拿到揭曉按鈕。當堂活動夠用，但別放需要保密的內容，也別在會被惡搞的場合單靠它。

---

## 資料存在哪裡

Supabase 專案 `qdpklfsrgdtgbsutckib`，兩張表。

**`public.wordcloud_entries`** —— 每一筆投稿一列：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `id` | bigint | 主鍵 |
| `room` | text | 主題代號，最長 40 字 |
| `word` | text | 詞彙，1–30 字 |
| `created_at` | timestamptz | 送出時間 |

**`public.wordcloud_rooms`** —— 每個主題一列，存「揭曉了沒」：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `room` | text | 主鍵，主題代號 |
| `revealed` | boolean | 揭曉狀態。老師按下按鈕就是改這一格 |
| `updated_at` | timestamptz | 最後更動時間 |

揭曉狀態存在資料庫而不是瀏覽器裡，有兩個原因：一是要能推播到每一支手機，二是**晚到的學生也要拿到正確狀態** —— 上課到一半才掃碼進來的人，如果老師已經揭曉了，他一進來就該直接看到文字雲。

兩張表都開了 RLS（anon 可讀寫）並掛上 Realtime，所以任何一台裝置的動作，其他人的畫面會立刻更新。

金鑰寫在 `index.html` 最上面的 `SUPABASE_URL` / `SUPABASE_KEY`。那是 publishable key（可公開），不是 service role key。

**Supabase 連不上時**會自動退成離線模式：詞只存在瀏覽器的 localStorage，狀態列會顯示「離線模式（只存在這台裝置）」。這時候多人是不會同步的。

## 載具

手機、平板、桌機都可以用，學生端和管理端都是。

- **平板直式（iPad 768px）**：刊頭放不下「標題 ＋ 四顆按鈕」一整排，所以工具列會自動掉到第二列、按鈕拉寬平分。
- **觸控裝置**：用 `@media (pointer:coarse)` 偵測，輸入框與送出鈕加高到 54px 以上、一般按鈕 44px、「撤回」連結也加了上下留白，手指按得準。滑鼠使用者不受影響。
- 輸入框字級一律 16px 以上，iOS Safari 才不會一點下去就自動放大整頁。

## 上限

- 一個主題最多讀 **1000 筆**（PostgREST 單次查詢上限）。超過就只保留最新的 1000 筆。
- 畫面最多嘗試排入 **200 個不同的詞**；真的塞不下時，統計列會誠實寫出「畫面放得下最熱門的 N 個」，優先留下票數高的。
- 免費方案的 Supabase 專案閒置太久會被暫停，`supabase-keepalive` 排程會定期戳一下。

## 清空資料

管理模式的按鈕最方便。要整批處理也可以直接下 SQL：

```sql
-- 清掉某一個主題
delete from public.wordcloud_entries where room = '六年一班';

-- 清掉全部
delete from public.wordcloud_entries;

-- 把某個主題收回成「未揭曉」（等同按下「收回文字雲」）
update public.wordcloud_rooms set revealed = false where room = '六年一班';
```

---

## 本機預覽

`.claude/launch.json` 已經加了 `wordcloud` 設定（port 5513）。用 preview_start 開啟即可。

不要直接用 `file://` 開檔 —— 那樣沒有網址可以做 QR Code，畫面會提示你先部署。

## 視覺規範

奶油紙底 × 印章紅 × 明體，走刊物印刷的調性。色票定義在 `index.html` 最上面的 `:root`：

| 用途 | 色碼 |
|---|---|
| 主背景（暖奶油） | `#F6F0E6` |
| 紙面（舞台、卡片） | `#FDFAF3` |
| 品牌紅（印章紅） | `#A61B22` |
| 深酒紅 | `#7C1015` |
| 主文字（暖黑） | `#2B2523` |
| 分隔線 | `#E2D7C6` |

字體：標題與文字雲用 **Noto Serif TC**（明體），介面文字用 Noto Sans TC。

文字雲的顏色分兩層 —— 票數最高的詞固定用品牌紅，其餘依熱度從紅色家族或暖色系陪襯色中取；顏色由詞彙 hash 決定，所以重排時同一個詞不會變色，除非它的票數竄升到領銜層。字重也跟票數走（500 / 700 / 900）。

淺色底要注意**小字的對比**：透明度下限設在 0.85，最小對比 4:1，全部通過 WCAG AA 大字標準（3:1）。若日後要調配色，記得重新驗一次，深色底沒事的組合換到奶油底上很容易被洗掉。

## 技術筆記

- **排版**：自己寫的演算法。詞由大到小依序排，每個詞沿阿基米德螺線從中心往外找第一個不重疊的位置（AABB 碰撞偵測）。全部排完若還有詞放不下，就整體縮小字級重排，最多 3 次。
- **顏色與方向**：用詞彙本身的 hash 決定，所以同一個詞每次重排的顏色都一樣，不會跳來跳去。中文詞有 1/5 機率直排（`writing-mode: vertical-rl`）。
- **四周留白**：文字距離框線至少 `min(寬,高) × 5.5%`（16–48px），字級也是按「扣掉留白之後」的面積推估的。
- **舞台一變高矮就要重排**：用 `ResizeObserver` 盯著舞台元素，不能只聽 `window.resize` —— 關鍵字面板一展開就會把舞台壓矮兩三百 px，只靠視窗事件的話底下的字會被切掉。
- **關鍵字合併不做半套**：`Intl.Segmenter` 會把「閱讀理解」切成兩段，所以要把原文中真正相鄰的中文詞黏回去，整串 4 字以內才合併。早期版本是貪婪地黏到滿 4 字就停，「培養批判思考」因此變成「培養批判」＋落單的「思考」，比不黏還糟；現在超過 4 字就完全不黏，只把落單的單字黏在一起（否則會被單字過濾器丟掉）。
- **量測必須跟渲染一致**：碰撞偵測用 canvas `measureText`，字型與字重要跟實際 DOM 完全相同。改字體或改字重規則時，`measure()` 裡的 `measurer.font` 一定要一起改，否則會開始重疊。
- **動畫**：元素依詞彙重複使用，位置與字級用 CSS transition 過渡，所以票數增加時字會平滑長大，而不是整片重畫。
- **爆量處理**：連續進來的投稿會合併成一次重排（160ms 內），避免全班同時送出時畫面卡住。
- **未揭曉時不排版**：學生端在等待狀態下完全不跑排版演算法，省手機效能，也讓詞彙不會出現在 DOM 裡。副作用是揭曉那一刻所有詞會一起播進場動畫，效果反而更好。
- **QR 函式庫**：qrcodejs（MIT）直接內嵌在 HTML 裡，學校網路擋掉 CDN 也不影響掃碼。
