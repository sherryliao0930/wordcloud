# 互動文字雲

大家一起輸入詞彙，畫面上即時長出一朵文字雲。同一個詞被越多人送出，字就越大。

檔案只有一個：`index.html`。整站沒有建置步驟，複製到任何靜態空間就能跑。

---

## 怎麼用

| 元件 | 說明 |
|---|---|
| **輸入框** | 打一個詞按 Enter。用逗號（`,` `，` `、`）可以一次送好幾個，單次最多 10 個、每個詞最長 30 字。 |
| **隱藏／顯示文字雲** | 右上角的按鈕。藏起來的時候只顯示「目前已收集 N 筆」，大家還是可以繼續投稿 —— 適合先蒐集、最後一次揭曉。 |
| **QR Code** | 右上角按鈕打開，顯示這一頁的網址與 QR。學生掃了就直接進到同一個主題。 |
| **投影模式** | 收掉輸入框、進入全螢幕，只留文字雲。滑鼠移到上方仍可叫回工具列。 |
| **撤回我上一個詞** | 打錯字時用。只能撤回自己在這台裝置送出的詞。 |

## 主題（同時開好幾場）

網址加上 `?room=代號` 就是一個獨立的文字雲，彼此不互相干擾：

```
https://你的網址/?room=六年一班
https://你的網址/?room=期末回饋
```

沒帶參數時用的是 `default` 這個主題。QR Code 會自動帶上目前的主題代號。

## 管理模式

網址加上 `?admin=1`，輸入框上方會多出一條管理列，可以**切換主題**和**清空這個主題**。

```
https://你的網址/?admin=1
```

學生掃 QR 拿到的網址**不會**帶 `admin=1`（QR 產生時會自動拿掉），所以他們看不到清空按鈕。

> 這只是「不讓人一眼看到」，不是真的權限保護。資料庫對匿名使用者是開放讀寫的，懂技術的人還是能繞過。別放需要保密的內容。

---

## 資料存在哪裡

Supabase 專案 `qdpklfsrgdtgbsutckib`，資料表 `public.wordcloud_entries`：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `id` | bigint | 主鍵 |
| `room` | text | 主題代號，最長 40 字 |
| `word` | text | 詞彙，1–30 字 |
| `created_at` | timestamptz | 送出時間 |

RLS 已開啟，anon 角色可讀、可新增、可刪除。Realtime 已掛上這張表，所以任何一台裝置送出，其他人的畫面會立刻更新。

金鑰寫在 `index.html` 最上面的 `SUPABASE_URL` / `SUPABASE_KEY`。那是 publishable key（可公開），不是 service role key。

**Supabase 連不上時**會自動退成離線模式：詞只存在瀏覽器的 localStorage，狀態列會顯示「離線模式（只存在這台裝置）」。這時候多人是不會同步的。

## 上限

- 一個主題最多讀 **1000 筆**（PostgREST 單次查詢上限）。超過就只保留最新的 1000 筆。
- 畫面最多嘗試排入 **200 個不同的詞**；真的塞不下時，統計列會誠實寫出「畫面放得下最熱門的 N 個」，優先留下票數高的。
- 免費方案的 Supabase 專案閒置太久會被暫停，`supabase-keepalive` 排程會定期戳一下。

## 清空資料

管理模式的按鈕最方便。要整批處理也可以直接下 SQL：

```sql
-- 清掉某一個主題
delete from public.wordcloud_entries where room = '六年一班';

-- 清掉全部
delete from public.wordcloud_entries;
```

---

## 本機預覽

`.claude/launch.json` 已經加了 `wordcloud` 設定（port 5513）。用 preview_start 開啟即可。

不要直接用 `file://` 開檔 —— 那樣沒有網址可以做 QR Code，畫面會提示你先部署。

## 技術筆記

- **排版**：自己寫的演算法。詞由大到小依序排，每個詞沿阿基米德螺線從中心往外找第一個不重疊的位置（AABB 碰撞偵測）。全部排完若還有詞放不下，就整體縮小字級重排，最多 3 次。
- **顏色與方向**：用詞彙本身的 hash 決定，所以同一個詞每次重排的顏色都一樣，不會跳來跳去。中文詞有 1/5 機率直排（`writing-mode: vertical-rl`）。
- **動畫**：元素依詞彙重複使用，位置與字級用 CSS transition 過渡，所以票數增加時字會平滑長大，而不是整片重畫。
- **爆量處理**：連續進來的投稿會合併成一次重排（160ms 內），避免全班同時送出時畫面卡住。
- **QR 函式庫**：qrcodejs（MIT）直接內嵌在 HTML 裡，學校網路擋掉 CDN 也不影響掃碼。
